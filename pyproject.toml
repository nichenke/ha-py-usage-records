[project]
name = "ha-usage-records"
version = "0.1.0"
description = ""
authors = [
    {name = "Nic Henke",email = "nichenke@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pydantic (>=2.10.5,<3.0.0)",
    "logfire (>=3.2.0,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]

[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
pytest = "^8.3.4"

